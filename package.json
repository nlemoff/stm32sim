{
  "name": "stm32sim",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:client": "bun build src/client/index.html --outdir=dist --minify",
    "dev": "bun run build:client && bun run src/server/index.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10"
  },
  "dependencies": {
    "@codemirror/lang-cpp": "^6.0.3",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "codemirror": "^6.0.2"
  }
}
