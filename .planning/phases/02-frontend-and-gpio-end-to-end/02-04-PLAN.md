---
phase: 02-frontend-and-gpio-end-to-end
plan: 04
type: execute
wave: 3
depends_on: [02-03]
files_modified: []
autonomous: false
requirements: [EDIT-01, EDIT-02, EDIT-03, GPIO-01, GPIO-02, GPIO-03, GPIO-04, CTRL-01, CTRL-02, CTRL-03]

must_haves:
  truths:
    - "All Phase 2 success criteria verified by human in the browser"
    - "Write-compile-run-see loop works end-to-end"
    - "GPIO input via virtual buttons works end-to-end"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete Phase 2 browser experience -- the write-compile-run-see loop with GPIO visualization and interactive input.

Purpose: Frontend UI behavior (visual rendering, interactive flows, real-time updates) cannot be verified by automated tests alone. This checkpoint confirms the complete user experience works as intended before marking Phase 2 complete.

Output: Confirmation that all Phase 2 success criteria are met, or a list of issues to address.
</objective>

<execution_context>
@/home/nlemo/.claude/get-shit-done/workflows/execute-plan.md
@/home/nlemo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-frontend-and-gpio-end-to-end/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of complete Phase 2 browser UI</name>
  <files></files>
  <action>
    Present the following verification checklist to the user. The server should already be runnable with `cd /home/nlemo/stm32sim && bun run dev`. Ensure the build is current with `bun run build:client` before asking the user to verify.

    This is a checkpoint task -- no code changes. The user tests the complete Phase 2 UI in their browser.
  </action>
  <verify>User provides "approved" or describes issues to fix.</verify>
  <done>All 7 test groups below pass human verification.</done>
  <what-built>Complete Phase 2 browser UI: code editor with C syntax highlighting, file upload, sample loading, GPIO visualization (LEDs + pin table), virtual input buttons, simulation controls (run/stop/speed/status), and compilation error display.</what-built>
  <how-to-verify>
    Start the server: `cd /home/nlemo/stm32sim && bun run dev`
    Open http://localhost:3000 in a browser.

    **Test 1: Editor and samples (EDIT-01, EDIT-03)**
    1. Verify the editor shows C code with syntax highlighting (keywords colored, strings colored)
    2. Select "blink" from the sample dropdown -- verify the blink sample code loads
    3. Select "knight-rider" -- verify the knight-rider code loads

    **Test 2: File upload (EDIT-02)**
    4. Create a test .c file on your machine with some C code
    5. Click the upload button and select it -- verify the content loads into the editor

    **Test 3: Compile and run blink (GPIO-02, GPIO-03, CTRL-01, CTRL-02)**
    6. Load the "blink" sample
    7. Click "Run" -- verify status shows "compiling" briefly, then "running"
    8. Verify virtual LEDs appear and blink (PA5 toggling)
    9. Verify the pin table shows PA5 as output with state toggling HIGH/LOW
    10. Click "Stop" -- verify status shows "stopped" and LEDs stop

    **Test 4: Speed control (CTRL-03)**
    11. Select "5x" speed from the dropdown
    12. Click Run on the blink sample -- verify LEDs blink noticeably faster
    13. Stop the simulation

    **Test 5: Virtual buttons / GPIO input (GPIO-04, GPIO-01)**
    14. Load the "button-led" sample
    15. Click "Run"
    16. Verify a virtual button appears for PA0 (the input pin)
    17. Click and hold the button -- verify LED PA5 turns on
    18. Release the button -- verify LED PA5 turns off

    **Test 6: Compilation errors**
    19. Clear the editor, type invalid C code (e.g., `int main() { undefined_function(); }`)
    20. Click Run -- verify compilation errors appear in the error panel below the editor
    21. Verify status shows "error"

    **Test 7: Second run (Pitfall 2 check)**
    22. Load the blink sample again and click Run
    23. Verify LEDs from previous run are cleared before new ones appear
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed and what you observed.</resume-signal>
</task>

</tasks>

<verification>
All 7 test groups above must pass. This verifies all Phase 2 success criteria from the ROADMAP:
1. User can write/upload/load code (Tests 1-2)
2. User sees LEDs and pin table updating live (Test 3)
3. Virtual buttons send input to firmware (Test 5)
4. Run/stop controls and status indicator work (Tests 3, 6)
5. Compilation errors display clearly (Test 6)
</verification>

<success_criteria>
- Human approves all 7 test groups
- No visual glitches, broken layouts, or unresponsive controls observed
- The "aha moment" works: write code, click run, see behavior visualized
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-and-gpio-end-to-end/02-04-SUMMARY.md`
</output>
